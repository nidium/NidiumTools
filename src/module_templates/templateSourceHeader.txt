#pragma once

#include <Binding/JSExposer.h>

class NativeJS{classname} : public Nidium::Binding::JSExposer<NativeJS{classname}>
{{
    public:
        NativeJS{classname}(JS::HandleObject obj, JSContext *cx);
        virtual ~NativeJS{classname}();
    private:
}};
