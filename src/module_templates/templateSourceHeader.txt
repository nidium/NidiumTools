#pragma once

#include <Binding/JSExposer.h>

class JS{classname} : public Nidium::Binding::JSExposer<JS{classname}>
{{
    public:
        JS{classname}(JS::HandleObject obj, JSContext *cx);
        virtual ~JS{classname}();
    private:
}};
